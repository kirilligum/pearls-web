import { Button } from 'shadcn';

export default function DApp() {
  return (
    <div>
      <h1>dApp</h1>
      <p>This is the dApp page. Check out our button below:</p>
      <Button>Click Me</Button>
    </div>
  );
}
